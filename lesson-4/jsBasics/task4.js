// task 4.1
{
    let radius = 10;
    let circleSquare = Math.PI * radius ** 2;
    console.log((circleSquare).toFixed(2));
}

// task 4.2 
{
    let length = 2.34;
    let width = 1.45;
    let rectangleSquare = length * width;
    console.log((rectangleSquare).toFixed(2));
}

// task 4.3
{
    let radius = 5.99;
    let height = 6.32;
    let cylinderVolume = Math.PI * radius ** 2 * height;
    console.log((cylinderVolume).toFixed(2));
}